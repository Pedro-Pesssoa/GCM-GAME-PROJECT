# Configurações do npm para o projeto QuizPlanet

# Salva exatamente as versões instaladas no package-lock.json
save-exact=false

# Usa o registry padrão do npm
registry=https://registry.npmjs.org/

# Instala dependências de desenvolvimento também
production=false

# Define o nível de log
loglevel=warn

# Define o engine-strict para garantir compatibilidade
engine-strict=true
